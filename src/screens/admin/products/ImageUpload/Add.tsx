import { IconWrapper } from "../Products.styled";
import AddIcon from "@mui/icons-material/Add";
import { Icon } from "@mui/material";

const Add = () => {
  return (
    <IconWrapper>
      <Icon component={AddIcon} />
    </IconWrapper>
  );
};

export default Add;
